Позитивные проверки 
@pytest.mark.positive
входные данные корректны.
capitalize
Логика не нарушена первая буква заглавная остольные строчные
"Skypro"
Строка с пробелом первая буква заглавная 
"Hello world"
Строка начинается с цифры ничего не меняется 
"1abc"
trim удаление пробельных символов только в начале строки.
"   skypro" → "skypro" — пробелы в начале успешно убраны.
"    hello" → "hello" — несколько пробелов в начале убраны.
" python" → "python" — один пробел в начале убран.
"no_spaces" → "no_spaces" — строка без ведущих пробелов остаётся неизменной.

негативные проверки
@pytest.mark.negative
"123abc" → "123abc"стока начинается не с буквы
"" → "" пустая строка
"   " → "   " пробелы
"ABC" все буквы заглавные


